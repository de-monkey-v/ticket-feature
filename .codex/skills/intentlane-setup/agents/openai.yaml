interface:
  display_name: "Intentlane Setup"
  short_description: "Onboard a fresh clone and run safely"
  default_prompt: "Use $intentlane-setup to take this repository from fresh clone to first successful run, generate a safe `.env`, and verify health and login."
